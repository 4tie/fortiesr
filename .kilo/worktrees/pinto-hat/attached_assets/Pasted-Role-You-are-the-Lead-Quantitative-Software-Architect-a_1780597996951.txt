Role: You are the Lead Quantitative Software Architect and Senior Full-Stack Developer for our algorithmic trading platform.

Task: Write an exhaustive, highly detailed, and completely unsummarized Markdown documentation file explaining the entire "AutoQuant Hybrid Pipeline" system. Leave absolutely nothing out. Every logic gate, every parameter, every state change, and every UI component must be thoroughly documented.

Scope of Documentation:

1. The Backend (The 7-Stage Pipeline):
For EVERY single stage of the 7 stages listed below, you must provide:

Objective: What is the exact purpose of this stage?

Input/Output: What data/state goes in, and what exactly comes out?

Core Logic & Algorithms: Explain the mathematical or logical mechanics in deep detail.

Error Handling & Fallbacks: What happens if a pair fails? How does the pipeline react?

The 7 Stages to detail:

Stage 1: Pre-Flight Filtering (stages_validation.py -> _filter_preflight_pairs): Explain the data gap detection and how the PF/trades baseline works.

Stage 2: WFA Optimization (stages_optimization.py): Explain Walk-Forward Analysis and explain exactly how the "Hard Mutation on segment failures" algorithm functions.

Stage 3: Robustness Check (stages_validation.py): Explain the "Auto-patch" mechanism and the exact mathematical formula/logic behind compute_stability_score (which results in state.stability_score).

Stage 4: OOS Validation (stages_validation.py): Explain Out-of-Sample validation and the saving mechanism for stage4_result.

Stage 5: Portfolio Competition (stages_assessment.py): Explain the Joint backtest. Detail the exact mathematical calculation for "ATR + StabilityScore inverse-vol weights" and why it is non-fatal if skipped.

Stage 6: Risk Assessment (stages_assessment.py): Explain how portfolio-level risk and max drawdown are evaluated.

Stage 7: Delivery (stages_assessment.py): Explain how the system dynamically injects custom_stoploss, custom_stake_amount, and stability_score as class attributes into the final Freqtrade strategy file.

2. The Frontend (AutoQuantTab.jsx):
Detail the React component structure and UI/UX workflow:

State Management: How does the UI track the live progress of the backend pipeline?

Component Details: Explain the logic behind:

Phase label badges on cards.

StabilityScoreBadge: Explain the 0–100 progress bar logic (green ≥70 / orange ≥40 / red below) and how it updates live during Stage 3.

PortfolioCompetitionPanel: Explain the pair allocation bar chart and the portfolio profit/trades summary.

Live Pipeline View: How the unified "Phase 3 — Robustness & Stability" card merges multiple metrics.

Graceful Degradation: Explain how the UI handles and displays the "skipped" message for Stage 5.

Output Format Constraint:

Use raw Markdown only.

Use standard Markdown syntax (headings #, ##, ###, bullet points -, bold **, and code blocks ```python or ```jsx for snippets where necessary).

Do NOT skip details for the sake of brevity. I want an exhaustive master document.