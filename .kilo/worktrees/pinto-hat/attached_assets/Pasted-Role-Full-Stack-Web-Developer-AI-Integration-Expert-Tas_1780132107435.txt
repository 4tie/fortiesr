Role: Full-Stack Web Developer & AI Integration Expert.

Task: Implement a new "Explain My Strategy" (AI Assistant) feature in the application. This feature will read a Python strategy file and explain its logic. 
CRITICAL CONSTRAINT: You MUST strictly use a local LLM via **Ollama**. Do NOT use OpenAI, Anthropic, or any external paid APIs.

### Phase 1: Backend Implementation (FastAPI + Ollama)
1. **Create the Endpoint:** In a suitable router (e.g., `backend/api/routers/ai_assistant.py`), create a `POST /api/ai/explain-strategy` endpoint that accepts a `strategy_name`.
2. **Read the File:** The endpoint should read the physical Python file of the requested strategy from the `user_data/strategies/` directory.
3. **Ollama Integration:** - Use the `httpx` or `requests` library (or the `ollama` python package if available) to make a call to the local Ollama instance at `http://localhost:11434/api/generate`.
   - The default model should be `llama3` (or allow it to be passed via config).
   - **The System Prompt for Ollama:** Construct a prompt that says: "You are a friendly trading mentor. Read the following Freqtrade Python strategy code. Explain its core logic (buy conditions, sell conditions, and indicators used) in simple, non-technical Arabic. Use real-world analogies like buying/selling a car or real estate to explain the concepts. Keep it concise."
4. **Return Response:** Return the generated text to the frontend. Ensure you handle potential timeouts or connection errors (e.g., if Ollama is not running on the user's machine).

### Phase 2: Frontend Implementation (React)
1. **The Trigger Button:** In the `PerformanceTab` or the Strategy selector component, add a new button with an AI/Brain icon (e.g., "🧠 أشرح لي" or "Explain Logic").
2. **The Output Modal/Drawer:** When the user clicks the button:
   - Show a clear "Loading..." state with a spinner (since local LLMs might take a few seconds to stream/generate the response).
   - Once the response is received, display it in a beautifully formatted Modal or Drawer with a clean typography (using Markdown rendering if possible, as the LLM might return bolded text or bullet points).
3. **Error Handling UI:** If the backend returns a connection error, show a friendly toast/alert: "Could not connect to local AI. Please ensure Ollama is running."

Please provide a brief execution plan detailing the Ollama connection method you will use, then proceed with the backend code.