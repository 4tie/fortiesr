Role: Senior AI Core Integration Engineer & Quant DevOps.

Context: We have a fully functional `Stress Test Lab` with three modes programmed (Time Split / Walk-Forward, Monte Carlo, and Crash Gauntlet). We need to seamlessly connect the outputs of these stress tests directly into our newly created Meta-Learning Ledger (`data/lessons_learned.json`) so the AI Assistant becomes fully aware of stress-test vulnerabilities and avoids them during autonomous strategy generation.

Task: Integrate Stress Test Lab results with the AI Meta-Learning framework.

### 1. Stress Test Outcome Interception (Backend Integration)
- Locate the execution router/worker handling the background process for `Launch Stress Test Lab`.
- Upon successful completion of a stress test run, intercept the final analytical summary payload:
  - `Consistency Score`
  - `Crash Gauntlet` results per scenario (COVID, China Ban, Luna, FTX, etc.)
  - `Max Drawdown Variance`

### 2. Auto-Logging Failures into `data/lessons_learned.json`
Implement an automated logging rule inside the post-test evaluation pipeline:
- **Condition A (Consistency Failure):** If the `Consistency Score` from the Time Split (Walk-Forward) is less than 70% (0.70), trigger an automated log entry into `data/lessons_learned.json`. 
  - *Format:* `{"date": "...", "source": "Stress Test Lab - Time Split", "strategy": "...", "lesson": "Strategy failed walk-forward consistency with a score of [X]%. Parameters are over-fitted to past data and lack structural adaptability across consecutive chronological windows."}`
- **Condition B (Black Swan Vulnerability):** If any specific historical scenario in the `Crash Gauntlet` results in a negative net profit OR a `Max Drawdown` exceeding 25%, log it immediately.
  - *Format:* `{"date": "...", "source": "Stress Test Lab - Crash Gauntlet ([Scenario Name])", "strategy": "...", "lesson": "Strategy was wiped out/severely damaged during the [Scenario Name] event with a drawdown of [X]%. Vulnerability identified: Severe exposure to high-volatility liquidations. Needs dynamic trailing stoploss or asset-correlation protective rules to survive systemic market crashes."}`

### 3. Loop Closure (AI Prompt Amplification)
- Ensure that when `backend/api/routers/ai_chat.py` injects the `<CRITICAL_PAST_LESSONS>` block from `data/lessons_learned.json` into the OpenRouter System Prompt, these newly captured Stress Test failure logs are parsed and highlighted.
- Instruct the AI persona: "If a past lesson originates from the 'Stress Test Lab', treat it as a critical operational boundary. When generating or modifying any strategy, you must explicitly inject structural code defenses (e.g., volume spikes filtering, tight ATR-based trailing exits) to address the specific historical crash or consistency issue recorded."

Execute the wiring cleanly without blocking the UI loading spinners, verify JSON schema appending, and output a confirmation.