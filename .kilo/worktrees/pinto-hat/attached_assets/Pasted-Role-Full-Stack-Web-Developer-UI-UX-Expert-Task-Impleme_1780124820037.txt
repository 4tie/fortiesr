Role: Full-Stack Web Developer & UI/UX Expert.

Task: Implement the "Optimizer Tab" in the Web Frontend using the existing, pre-built Python backend architecture. Do NOT create new backend business logic files; leverage the existing infrastructure.

### INSTRUCTION 1: WRITE AN EXECUTION PLAN FIRST
Before writing any code, write a comprehensive, step-by-step implementation plan. Wait for my confirmation after the plan before writing the actual code. The plan must detail:
1. Which existing frontend routing/navigation file will be updated to add the new "Optimizer" tab.
2. The structure of the new Frontend components (Layout, Sidebar, Charts, Tables).
3. How frontend polling (every 300ms) will be hooked into the existing `LogicManager` / API endpoints to fetch the active session status.

---

### INSTRUCTION 2: FRONTEND REQUIREMENTS (Once approved)
Build a state-of-the-art web interface for the Optimizer Tab using a professional, modern dark-themed Trading Dashboard layout (similar to the provided layout specs):

1. **Three-Pane Layout Layout:**
   - **Left Sidebar:** * Configuration inputs (Strategy selector, Timeframe, Timerange, Pairs, Total Trial count, Search Strategy, Score Metric) matching the `OptimizerSessionConfig` model.
     * Parameters Table: A table listing available strategy parameters with checkboxes to enable/disable them for optimization, showing min/max/step/default values.
   - **Center Pane:**
     * Progress indicators: Visual progress bar showing percentage completion, elapsed time, and dynamic ETA calculated from the backend session JSON.
     * Live Logs: A timestamped rolling log component displaying trial events.
     * Trial Results Table: A dynamic table showing completed/running/failed trials with columns: Trial #, Status (color-coded), Composite Score, Profit %, Drawdown %, Trades.
   - **Right Sidebar:**
     * Best Result Panel: Displays top summary metrics from the highest-scoring trial (Profit, DD, Score) with an "Export/Apply" button.
     * Selected Trial Panel: When a row in the Center Table is clicked, shows parameter differences compared to defaults.

2. **Real-time Charts Integration:**
   - Implement two live-updating line charts in the center pane (using the frontend's standard charting library like Chart.js, Recharts, or ApexCharts):
     * Chart 1: Net Profit % per completed iteration (Green line).
     * Chart 2: Max Drawdown % per completed iteration (Red line).
   - Ensure the charts update instantly during the 300ms polling cycle when a new trial completes.

3. **Backend Integration Rules:**
   - Do NOT create new optimization algorithms or logic services.
   - Strictly use and interface with the existing backend components: `backend/models/optimizer.py`, `backend/services/strategy/strategy_optimizer.py`, and endpoints exposed through `app/controllers/logic_manager.py`.
   - Implement frontend filter logic to automatically discard/hide temporary or blank duplicate "0" rows that emit during a trial's initialization phase (only show rows when status is fully COMPLETED, RUNNING, or FAILED).

Please begin by outputting the requested execution plan in Step 1.