Role: Principal QA Automation Engineer & Freqtrade DevOps Expert

Task: Perform an End-to-End (E2E) integration and execution test of the complete 5-phase OmniFactory AutoQuant pipeline. Monitor all logs, dynamically intercept and fix any runtime errors, and verify a 100% successful generation.

Please execute this task using the following execution and debugging framework:

### 1. END-TO-END PIPELINE SIMULATION & LOG ANALYSIS
- Initialize and trigger the entire pipeline workflow sequentially:
  - Phase 1: Data Validation & Auto-Download (Verify unified input field and gap detection).
  - Phase 2: Baseline Backtest & Pre-Filtering (Verify 15-trade gate, division-by-zero protection, and the max 3-retry self-healing loop).
  - Phase 3: Robustness & Feature Injection (Verify global portfolio stress runs at 1x, 2x, 3x fees and safe file modification blocks).
  - Phase 4: Portfolio Capital Competition (Verify joint backtest execution, capital starvation tracking, and equal-weight normalization fallback).
  - Phase 5: ATR Sizing & Final Output (Verify injection of atr/stability dicts, .get() guards, and min_stake overrides).
- Actively tail and analyze stdout, stderr, and Python traceback logs throughout the run.

### 2. DYNAMIC BUG FIXING & ERROR RESOLUTION
- If any stage halts, returns a non-zero exit code, raises a syntax/runtime exception, or encounters a WebSocket state synchronization failure:
  - IMMEDIATELY intercept the failure, analyze the traceback error source.
  - Programmatically rewrite or patch the faulty logic inside the orchestrator or pipeline modules to resolve the issue.
  - Reset and re-execute the workflow cleanly. Do not hand back an incomplete or broken script. Ensure the pipeline runs 100% flawlessly.

### 3. WORKFLOW EXPLANATION SUMMARY
- Once the pipeline successfully delivers the final clean `{StrategyName}_Optimized.py` deployment file, provide a comprehensive, clear, and scannable technical breakdown explaining:
  1. How data flows seamlessly from Phase 1 straight through to Phase 5.
  2. How the defensive guards handled edge cases during the test run.
  3. The exact directory structure where the logs, intermediate configs, and the final production-ready strategy file reside.

Run the end-to-end testing suite now and provide the fully optimized, validated, and debugged operational results.