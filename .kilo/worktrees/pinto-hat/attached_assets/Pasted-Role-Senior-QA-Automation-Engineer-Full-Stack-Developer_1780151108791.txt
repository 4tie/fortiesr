Role: Senior QA Automation Engineer & Full-Stack Developer.

Task: Implement a rigorous, end-to-end Validation and Test Suite for the newly added "Auto-Quant Factory" module, with a heavy emphasis on "Hyper-Detailed Logging" and error resilience.

### 1. Robust Logging Architecture (CRITICAL)
- In `backend/services/auto_quant/pipeline.py`, integrate Python's structured `logging` module. 
- Every single stage change, subprocess spawn, config read, and file write must emit a clear log entry with explicit log levels (`INFO`, `DEBUG`, `ERROR`).
- If an exception occurs, the logger MUST capture the full traceback (`logger.exception`) so we can pinpoint the exact line of failure.
- Ensure all backend logs are not only written to the server terminal but are also perfectly captured and pushed into the asyncio queue to stream via WebSockets to the frontend live terminal.

### 2. End-to-End Simulation & Verification
- Run a full end-to-end test of the pipeline using a mock/sandbox FreqAI setup or a fast mock backtest to verify that the workflow progresses seamlessly from Stage 1 to Stage 7.
- Test Failure Resilience: Manually inject a failure into Stage 4 (Overfitting Validation) and Stage 6 (Risk Check Failures) to verify that:
  1. The backend orchestrator gracefully halts the task.
  2. The exact error message and stage position are captured.
  3. The WebSocket broadcasts a `status: "failed"` payload.
  4. The frontend UI correctly renders the red "Failed" state and displays the precise error message.

### 3. WebSocket & Component Reconnect Test
- Verify that if the user refreshes the browser or loses connection mid-way through a long run, the frontend can fetch the state snapshot from `GET /api/auto-quant/status/{run_id}` and seamlessly re-establish the WebSocket connection without resetting the progress bar.
- Test the file generator outputs to ensure that `<StrategyName>_Optimized.py` and `config.json` are completely verified, syntactically valid, and the download endpoints respond with `200 OK`.

Please execute this complete diagnostic run. If you encounter any bugs, fix them immediately and output the clean terminal test results along with a snippet of the new detailed logs.