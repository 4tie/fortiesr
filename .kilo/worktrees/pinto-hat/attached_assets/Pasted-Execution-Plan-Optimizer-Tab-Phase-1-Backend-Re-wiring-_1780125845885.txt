Execution Plan: Optimizer Tab
Phase 1 — Backend Re-wiring (no new logic, only reconnect deleted wires)
#FileChange1backend/models/__init__.pyAdd from .optimizer import * so all optimizer model types are importable2backend/api/models.pyRestore OptimizerApiRequest (the HTTP-layer Pydantic model the router expects)3backend/app_services.pyRe-import OptimizerStore + StrategyOptimizer; add optimizer_store and strategy_optimizer attributes to AppServices4backend/api/routers/optimizer.pyAdd one new GET endpoint GET /api/optimizer/session/{optimizer_session_id} so the frontend can poll full trial data (list of OptimizerTrial objects) live during a run5backend/api/app.pyImport optimizer router and call app.include_router(optimizer.router)6backend/api/log_broadcaster.pyAdd services.strategy_optimizer.set_log_callback(broadcaster.write) inside wire_service_callbacks
Phase 2 — Navigation wiring
FileChangefrontend/src/components/NavPanel.jsxAdd "Optimizer" nav item (same pattern as other tabs)frontend/src/App.jsxImport OptimizerTab; add optimizer case to tab switch
Phase 3 — OptimizerTab.jsx — Three-pane layout
Left Sidebar (Config + Parameters)

Strategy selector dropdown (fetched from /api/strategies)
Timeframe, Timerange, Pairs inputs
Total Trials slider (1–500), Search Strategy select (random / grid / bayesian / evolutionary), Score Metric select (all 8 OptimizerScoreMetric values)
Parameters Table — lists the strategy's StrategyParameterDefinition entries with checkbox (enabled), min/max/step/default columns, editable inline
Center Pane (Progress + Charts + Trial Table + Logs)

Progress bar — completed_trials / total_trials percentage, elapsed time, live ETA from eta_seconds
Chart 1 — Net Profit % per completed trial (green line, Recharts LineChart)
Chart 2 — Max Drawdown % per completed trial (red line)
Trial Results Table — columns: Trial #, Status (color-coded badge), Score, Profit %, Drawdown %, Trades; filters out rows where status is PENDING and metrics are all null ("blank 0 rows"); clicking a row selects it for the right panel
Live Logs — rolling 200-line terminal box fed from /api/logs/stream SSE
Right Sidebar (Best + Selected)

Best Result Panel — shows metrics from 
highest-scoring trial (Profit, Drawdown, Score, Trades) and an "Apply to
 Strategy" button (writes best params to the strategy version)
Selected Trial Panel — appears when a trial row is clicked; shows each parameter value and highlights deltas from the strategy's current defaults
Phase 4 — Polling / API integration
EndpointUsed forGET /api/strategiesPopulate strategy selectorPOST /api/optimizer/runStart session → receive {session_id}GET /api/session/status/{session_id}Fast-path: poll every 300 ms for status, result.optimizer_session_id, completed_trials, phaseGET /api/optimizer/session/{optimizer_session_id}Poll every 300 ms for full OptimizerSession including all trials[] arrayGET /api/logs/stream (SSE)Live log lines fed into the log terminal box
Polling lifecycle: useEffect starts a 300 ms setInterval when a session is active; clears the interval when phase reaches completed / failed / cancelled.
That's the complete plan. Shall I proceed with implementation?