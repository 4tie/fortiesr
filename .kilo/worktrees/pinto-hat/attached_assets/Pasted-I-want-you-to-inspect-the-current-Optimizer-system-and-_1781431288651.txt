I want you to inspect the current Optimizer system and AutoQuant Factory integration points. Do not implement changes yet. Do not refactor yet. Only analyze and report with exact files, functions, and current behavior.

Main goal:
I need to understand exactly how the Optimizer Tab works today, how strategy files are extracted, how parameters are detected, whether there are duplicated buttons or duplicated workflows, and how optimized parameters could later be handed off to AutoQuant Factory.

Please inspect these areas:

1. Optimizer Tab current workflow
   Inspect:

* frontend/src/components/OptimizerTab.jsx
* backend/api/routers/optimizer.py
* backend/services/strategy/strategy_optimizer.py
* backend/services/strategy/strategy_optimizer_search.py
* backend/services/strategy/version_manager.py
* any related optimizer models/session/trial files

Answer:

* How does the user select a strategy?
* How does the user select or configure parameters?
* How are selected parameters sent to the backend?
* How are search spaces built?
* Does the optimizer optimize only selected parameters, or can it accidentally optimize all detected parameters?
* What happens when the user selects multiple parameters?
* How are Random/Grid/Bayesian/Evolutionary strategies applied to selected parameters?
* Where are trial params stored?
* Where are trial results stored?
* How is best trial selected?
* Does the optimizer currently support multiple pairs already selected by the user? If yes, where are those pairs passed and how are metrics aggregated?

2. Strategy file extraction and sidecar JSON extraction
   Inspect:

* backend/services/strategy/strategy_registry.py
* backend/services/strategy/strategy_source.py
* backend/api/routers/strategies.py
* any strategy file listing endpoints
* any params.json loading logic

Answer:

* How does the system list available strategy .py files?
* Does it correctly detect matching .json files next to the strategy?
* Does it load both .py and .json together?
* How does it extract parameters from the Python strategy file?
* How does it merge or compare Python defaults with sidecar JSON params?
* Does it support Freqtrade parameter spaces correctly: buy, sell, roi, stoploss, trailing?
* Does it support BooleanParameter, IntParameter, DecimalParameter, CategoricalParameter?
* Does it detect whether a parameter is actually used in strategy logic, or only detects that it exists?
* Does it validate the strategy before optimization?

3. Check duplicated buttons and duplicated workflows
   Inspect the frontend carefully.

Find any duplicated or confusing buttons related to:

* Run optimizer
* Apply trial
* Promote best trial to candidate
* View best params
* Validate strategy
* Export
* Save strategy
* Accept/Reject version
* AutoQuant start/submit buttons if they overlap with Optimizer actions

For each duplicated/confusing button, report:

* Button label
* File/component location
* What endpoint it calls
* Whether it modifies original strategy
* Whether it modifies accepted version
* Whether it only creates candidate version
* Whether it is safe or dangerous
* Recommendation: keep, rename, hide under advanced, or remove later

Important:
Do not remove buttons now. Only report.

4. Best trial params and candidate promotion
   Inspect the new optimizer endpoints if present:

* GET /api/optimizer/session/{id}/trial/{n}/params
* GET /api/optimizer/session/{id}/best-trial/params
* POST /api/optimizer/session/{id}/trial/{n}/promote-candidate
* POST /api/optimizer/session/{id}/best-trial/promote-candidate

Answer:

* Do these endpoints work from the frontend?
* Do they return structured Freqtrade-compatible params?
* Does the structured JSON look like:
  {
  "strategy_name": "...",
  "params": {
  "buy": {},
  "sell": {},
  "roi": {},
  "stoploss": {},
  "trailing": {}
  }
  }
* Does promote-candidate create a separate candidate version?
* Does it avoid modifying the original strategy file?
* Does it avoid changing the accepted version?
* Does it save both strategy.py and params.json?
* Are there any missing mappings for trailing, roi, stoploss, buy, or sell params?

5. Smart parameter selection analysis
   I do not want the optimizer to blindly optimize all parameters.

Analyze whether the current system has any logic for deciding:

* optimize now
* lock
* optimize later
* ignore
* conditional parameter

Answer:

* Does the current optimizer have a smart parameter selection engine?
* Or does it only optimize whatever the frontend/user sends?
* Does the UI clearly explain why only some parameters are selected?
* Example: if a strategy has 12 parameters but only buy_ma_count, buy_ma_gap, sell_ma_count, sell_ma_gap are optimized, where is that decision made?
* Is it frontend hardcoded?
* Backend generated?
* User selected?
* Derived from parameter names?
* Derived from Freqtrade spaces?
* Or manual only?

Also recommend a future design for:

* Auto mode: system suggests core entry/exit params first
* Manual mode: user selects params
* Risk mode: user optionally includes stoploss, roi, trailing
* Locked params: existing JSON values are preserved unless user chooses to optimize them

Do not implement this yet. Only analyze current behavior and propose a safe plan.

6. Difference between Optimizer and AutoQuant Factory
   Explain clearly and simply:

* What is Optimizer responsible for?
* What is AutoQuant Factory responsible for?
* Where do they overlap?
* Where should they not overlap?
* Should Optimizer choose pairs? Or should it use pairs already selected before it?
* Should Optimizer validate OOS/multi-pair/robustness? Or should that be AutoQuant Factory later?
* Should Optimizer generate strategies? Or only optimize a selected strategy?
* What should be the handoff point from Optimizer to AutoQuant Factory?

Expected answer:
Optimizer should optimize selected parameters for a selected strategy using the user-selected strategy, selected pairs, and selected timerange.
AutoQuant Factory should later validate/promote/export the candidate using deeper checks.

Please confirm whether the current code follows this separation or mixes responsibilities.

7. How to hand off selected parameters to AutoQuant Factory later
   Analyze how a candidate created from best trial could later be sent to AutoQuant Factory.

Answer:

* What object should be passed? candidate_version_id, strategy_name, params_json, run_id, or session_id?
* Does AutoQuant currently accept candidate versions?
* Does AutoQuant currently accept params from optimizer?
* What minimal endpoint would be needed later?
* Should the handoff be:
  Optimizer best trial → candidate version → AutoQuant validation run
* What files or services would need small changes later?
* What should not be changed yet?

8. Required final report format
   Return the report in these sections:

A. Simple summary in 10 lines.

B. Current Optimizer workflow:
User selects strategy/pairs/params → frontend → backend → trials → best trial → candidate.

C. File/function map:
Table with file, function/component, responsibility, current behavior.

D. Parameter extraction report:
How .py and .json are detected, parsed, merged, and exposed.

E. UI duplication report:
Table of buttons/actions, endpoints, safety level, and recommendations.

F. Parameter selection report:
What currently decides selected parameters, and whether smart selection exists.

G. Optimizer vs AutoQuant Factory:
Clear comparison table.

H. Handoff plan:
Minimal safe future path from optimizer candidate to AutoQuant Factory.

I. Risks:
What could accidentally modify original strategy or accepted version?

J. Recommended next step:
One small implementation step only, not a big refactor.

Important restrictions:

* Do not implement.
* Do not refactor.
* Do not delete buttons.
* Do not change API behavior.
* Do not modify AutoQuant.
* This is inspection and planning only.
