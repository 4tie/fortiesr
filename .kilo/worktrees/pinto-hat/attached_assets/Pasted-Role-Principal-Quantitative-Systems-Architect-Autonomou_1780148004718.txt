Role: Principal Quantitative Systems Architect & Autonomous AI Agent.

Objective: Squeeze your brain and utilize EVERY tool at your disposal (Web Search, File Reading, Sub-agents, Brainstorming, and Planning). I want you to architect and implement an "Auto-Quant Pipeline" (End-to-End Automated Workflow) within our current Freqtrade-based application.

The Goal: A user uploads/selects a base strategy and timeframe. They click "Start Auto-Quant". The system takes over completely and runs an automated pipeline that outputs a highly optimized, stress-tested, and "Live-Trading Ready" strategy file.

### Agentic Directives (CRITICAL):
1. USE WEB SEARCH: Look up "Freqtrade advanced hyperopt workflows", "Walk-forward analysis Python", and "Out-of-Sample testing quant trading" to gather the latest best practices before coding.
2. READ WORKSPACE: Analyze all currently available files in this project to understand the existing FastAPI/React structure, existing optimizer logic, and backtest endpoints.
3. PLAN FIRST: Output a strict execution plan before writing the massive code required.

### The Pipeline Architecture (The "Auto-Quant" Workflow):
You must build a new tab called "Auto-Quant Factory" that triggers a Background Task orchestrating this exact sequence:

1. Stage 1: Sanity Check (Initial Backtest)
   - Run a standard backtest on the user's selected strategy to ensure the code compiles and has basic logic. If it fails, stop and report the error.

2. Stage 2: Deep Hyperoptimization
   - Automatically trigger Freqtrade's `hyperopt`. Optimize ROI, Stoploss, Trailing Stop, and Strategy parameters for the selected timeframe.
   - Extract the best parameters from the Hyperopt JSON/Output.

3. Stage 3: Auto-Patching (Code Generation)
   - Dynamically inject the optimized parameters back into a NEW cloned Python strategy file (e.g., `UserStrategy_Optimized.py`).

4. Stage 4: Out-of-Sample Validation (Walk-Forward)
   - Run a backtest using the NEW strategy on a completely different time range (Out-of-Sample data) that Hyperopt did not see. 
   - If the strategy fails on unseen data (overfitting), discard it, and retry Stage 2 with different spaces, or mark it as "Failed Validation".

5. Stage 5: Multi-Pair Stress Test
   - Run the validated strategy across the top 20 volume pairs. Filter out pairs where the strategy loses money.

6. Stage 6: Final Risk Assessment
   - Apply Max Drawdown checks and Win/Loss consistency checks. 

7. Stage 7: The "Live Ready" Delivery
   - Present a complete report to the user.
   - Output the finalized, production-ready Python strategy file and an optimized `config.json` ready for a live Freqtrade bot.

### Technical Implementation Requirements:
- Long-Running Processes: This pipeline will take hours. You CANNOT use standard HTTP blocking requests. You MUST implement `BackgroundTasks` (or Celery) in FastAPI.
- Real-Time Updates: You MUST implement WebSockets (`fastapi.websockets`) to stream live terminal logs and progress bar updates (e.g., "Currently running Stage 3...") directly to the React frontend.

Do not ask for permission. Brainstorm, plan, use your tools, and start building the backend orchestration and frontend UI for this pipeline.