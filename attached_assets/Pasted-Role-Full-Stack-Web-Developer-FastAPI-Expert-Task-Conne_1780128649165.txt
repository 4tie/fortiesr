Role: Full-Stack Web Developer & FastAPI Expert.

Task: Connect the "Optimizer Tab" with the "Stress Test Lab" by implementing an advanced single-trial application and multi-trial export system. Maintain code cleanliness and leverage existing storage paradigms.

### Phase 1: Backend Endpoints & Logic Wiring
Create or extend the necessary FastAPI routers to handle single-apply and multi-export functionality:
1. `POST /api/optimizer/apply-trial` -> Accepts `strategy_name` and `parameters` (from a single trial). It updates the strategy's parameter file or active version with these new optimized values.
2. `POST /api/optimizer/export-trials` -> Accepts an array of selected trial configurations. Saves them into a central configuration registry file (e.g., `user_data/exported_optimizer_runs.json`) with customized labels indicating their origin (e.g., "[Opt] MultiMa - Trial #15 (Score: 42.5)").
3. `GET /api/optimizer/exported-trials` -> Returns the list of all exported trial parameter configurations so other tabs can consume them.
4. Update `temporal_stress_lab.py`: Modify the execution logic so that if a stress test is triggered using an "Exported Trial Configuration", it injects those specific custom parameters into the Freqtrade CLI subprocess runner request instead of using the strategy's defaults.

### Phase 2: Frontend Implementation (Optimizer Tab Updates)
Modify the `Trial Results Table` and the action panel in the Optimizer Tab:
1. **Checkbox Column:** Add a multi-select checkbox column to the far left of the Trial Results Table.
2. **Dynamic Button Behavior:** - If **only one row is highlighted/clicked** (and no checkboxes are ticked), the button text remains "Apply & Export" and updates the specific active strategy parameters via API.
   - If **multiple checkboxes are ticked**, change the primary button text to "Export Selected to Stress Test Lab". Clicking it fires the multi-export API and shows a success toast notification (e.g., "3 configurations exported to Stress Test Lab!").

### Phase 3: Frontend Implementation (Stress Test Lab Updates)
Modify the Strategy selection workflow inside the Stress Test Lab tab:
1. **Dropdown Segmentation:** Update the Strategy Selector dropdown component. It must now display two grouped sections:
   - Group 1: `Standard Strategies` (Lists default system strategies).
   - Group 2: `Exported from Optimizer` (Dynamically fetched via `GET /api/optimizer/exported-trials`).
2. **Execution Hook:** When the user selects a configuration from the "Exported from Optimizer" section and chooses an engine mode (Time Split, Monte Carlo, or Crash Gauntlet), pass the custom configuration payload to the backend task queue runner so the stress test uses the optimized parameters.

Please provide a clear, step-by-step technical plan specifying the data structures you will use before writing the frontend and backend code.