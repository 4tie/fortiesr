Role: Full-Stack Web Developer & UI/UX Expert.

Task: Upgrade the existing "Trading Pairs" input field inside the **Backtest Tab** into an interactive "Smart Pair Selector" component using the pre-built Python `PairSelectorService`. Do NOT create a new tab or a completely separate page.

### Phase 1: API Routing (Backend)
Ensure FastAPI routes exist to expose `PairSelectorService` actions to the frontend:
1. `GET /api/pairs/state` -> Returns full state (available, selected, favorites, locked, max_trades).
2. `POST /api/pairs/toggle-favorite` -> Toggles star status.
3. `POST /api/pairs/toggle-lock` -> Toggles padlock status.
4. `POST /api/pairs/toggle-select` -> Selects/Deselects a pair.
5. `POST /api/pairs/randomize` -> Triggers the randomize function based on max open trades.
6. `POST /api/pairs/update-max-trades` -> Updates the limit.
7. `POST /api/pairs/clear` -> Clears all non-locked pairs.

### Phase 2: Frontend Integration (Inside Backtest Tab)
Locate the existing form inside the **Backtest Tab**. Replace the standard multi-select or text input for "Trading Pairs" with an advanced inline dropdown or an expandable popover panel:

1. **The Display Input (Trigger):**
   - The main field should look like a premium input box that displays a summary of currently selected pairs (e.g., "BTC/USDT, ETH/USDT (+3 more)" or "5 Pairs Selected").
   - Clicking this field will toggle open the advanced selection panel directly underneath it (or as a clean modal/popover).

2. **The Advanced Selection Panel:**
   - **Top Controls Row:** Includes a search input to filter pairs, a numeric controller for "Max Open Trades", a "Randomize" button (🎲), and a "Clear" button.
   - **Scrollable Pairs List:** Displays rows of pairs. Each row must feature:
     - Pair Name (e.g., SOL/USDT).
     - **Star Icon (⭐):** To toggle favorite status (pinned to top or highlighted).
     - **Padlock Icon (🔒/🔓):** To lock the pair so it survives randomizations and clears.
     - **Checkbox/Switch:** To manually select or deselect the pair.

3. **State Management:**
   - When a user interacts with stars, locks, or hits Randomize (🎲), fire the background API call and immediately refresh the panel using the updated state from `pair_selector_state.json`.

Please write a brief execution plan detailing how you will modify the existing Backtest tab component before writing the code.