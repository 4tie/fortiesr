Goal:

Every Freqtrade strategy must have a matching sidecar JSON file next to it, and every export must always include both the Python strategy file and the JSON params file together.



Example expected structure:



user_data/

strategies/

MultiMa.py

MultiMa.json



If a strategy has no matching JSON file, the system should create a valid default JSON file for it



Then compare the current strategy/params JSON format with the latest stable Freqtrade documentation and the installed version actually used by the app.



Important:

Use the installed Freqtrade version as the runtime source of truth, but make sure the format is not outdated compared to current stable Freqtrade behavior.



Report:



Installed Freqtrade version

Expected strategy file requirements

Expected params JSON format

Whether our current .py + .json format is compatible

Any compatibility risks

Verify strategy discovery



Inspect:



strategy_registry.py

strategy_source.py

strategies.py

optimizer.py

version_manager.py

any frontend file that lists/selects strategies



Answer:



How does the app find strategies?

Does it scan user_data/strategies?

Does it ignore versions/ folders correctly?

Does it pair Strategy.py with Strategy.json?

What happens if Strategy.json is missing?

What happens if Strategy.json has a mismatched strategy_name?

Does the UI show whether a JSON file exists or not?

Enforce sidecar JSON existence



Inspect whether the app currently guarantees that every strategy has a sidecar JSON.



If it does not, propose and implement a safe small fix:



When scanning strategies or validating a selected strategy:



If Strategy.py exists but Strategy.json is missing:

Create Strategy.json next to it.

Do not modify Strategy.py.

Extract defaults from the Python strategy if possible.

If defaults are not available, create empty valid sections.



Default JSON format should be:



{

"strategy_name": "StrategyClassName",

"params": {

"buy": {},

"sell": {},

"roi": {},

"stoploss": {},

"trailing": {}

}

}



If the strategy has known values:



minimal_roi → params.roi

stoploss → params.stoploss.stoploss

trailing_stop fields → params.trailing

buy params → params.buy

sell params → params.sell



Important:

Do not invent random values.

Use Python defaults or empty sections.



Verify JSON merge logic



Inspect the parser behavior.



Answer:



Are Python defaults extracted?

Are JSON values loaded?

If both exist, which wins?

Is this behavior consistent everywhere?

Does optimizer use the same merged values?

Does export use the same values?

Are there duplicated merge functions that could produce different JSON?



Expected behavior:



Strategy.py defines defaults.

Strategy.json overrides defaults.

The app should display the final resolved params clearly.

Verify export behavior



Inspect all places that export, save, accept, promote, or apply strategy results:



optimizer promote candidate

optimizer apply trial

version manager accept version

AutoQuant delivery/export

strategy save/export

stress lab export if relevant



Answer for each:



Does it export Python file?

Does it export JSON params file?

Does it export both together?

Does it ever export only .py without .json?

Does it ever export only .json without .py?

Does it overwrite accepted params directly?

Does it create candidate safely?

Does it preserve original files?



Expected rule:

Any final strategy export must always include both:



StrategyName.py

StrategyName.json



If exporting a candidate or optimized version:



CandidateName.py

CandidateName.json



Do not allow a final export to contain only one of them.



Verify optimizer candidate promotion



Inspect the current promote-candidate workflow.



Answer:



Does Promote Best Trial to Candidate create both strategy.py and params.json?

Does the params.json follow Freqtrade-compatible structure?

Does it avoid changing the original strategy?

Does it avoid changing the accepted version?

If trial has no run_id, does it fail safely?

Is the UI clear that Promote is safe and Apply/Overwrite is destructive?

Verify dangerous actions



Inspect buttons and endpoints that can modify files.



Report:



Any button that overwrites accepted version params

Any button that saves over Strategy.py

Any button that exports only one file

Any endpoint that modifies accepted version without candidate

Any missing confirmation dialog



Expected:



Destructive actions must be clearly labeled.

Safe candidate promotion should be the default action.

Direct overwrite should be hidden behind confirmation.

Required final report



Return the report in this format:



A. Simple summary in 10 lines.



B. Freqtrade compatibility check:



installed version

expected format

current app compatibility

risks



C. Strategy file pairing report:

Table:



Strategy name

.py exists

.json exists

JSON valid

strategy_name matches

action needed



D. Missing JSON handling:



current behavior

recommended behavior

implemented small fix if needed



E. Export workflow report:

Table:



workflow/action

exports .py?

exports .json?

exports both together?

safe or dangerous?

recommendation



F. Optimizer candidate promotion report:



does it create both files?

does it preserve accepted version?

does it return candidate_version_id?



G. Issues found:



critical

important

nice to have



H. Recommended next step:

Only one small next step after this inspection.



Restrictions:



Do not implement smart parameter selection.

Do not change AutoQuant.

Do not refactor big modules.

Do not redesign UI.

Only inspect and make minimal safe fixes related to missing sidecar JSON and export consistency if necessary.