Role: Full-Stack Web Developer & UI/UX Expert.

Task: Implement the "Performance Tab" (Strategy History & Analytics Archive). This tab will allow users to select a strategy, view its entire backtest history, inspect deep metrics of past runs, and apply historical parameters to the active strategy using the existing Git/Version Manager system.

### INSTRUCTION 1: WRITE AN EXECUTION PLAN FIRST
Before writing any code, provide a detailed step-by-step implementation plan. Wait for my confirmation. The plan must cover:
1. Backend: How you will read/fetch historical backtest metadata and results (e.g., from `user_data/backtest_results` or existing history database) filtered by `strategy_name`.
2. Backend: Endpoint to apply a historical run's parameters to the current active strategy via the existing `version_manager` / Git control system.
3. Frontend: Layout of `PerformanceTab.jsx` featuring a strategy selector, analytics charts, a history table, and an inspection sub-panel/modal.

---

### INSTRUCTION 2: TECHNICAL REQUIREMENTS (Once approved)

1. **Top Control Bar:**
   - A dropdown selector to choose the target Strategy (fetched from `/api/strategies`).

2. **Main Dashboard Layout (Rendered upon selecting a strategy):**
   - **Analytics Section (Top):** A clean charting component showing performance trends over time across different backtest runs (e.g., Net Profit vs Max Drawdown trend lines).
   - **Historical Runs Table (Center):** Lists all past backtests performed for this strategy with columns: Run ID/Date, Strategy Version, Timeframe, Pairs Used, Net Profit %, Max Drawdown %, Total Trades, and an "Actions" column.

3. **Actions & Interactivity:**
   - **Inspect Run Button (🔍):** Clicking a row or an inspect button opens a detailed Modal or Drawer displaying the full results of that specific historical run (All trading pairs, dates, exact win/loss ratios, and the exact parameter block used during that test).
   - **Apply Parameters Button (⚙️ / Apply):** A button that triggers a `POST` request to the backend. The backend will extract the exact parameters used in that historical run and apply them directly to the active version of the strategy using your integrated Version/Git Manager. Show a success notification: "Parameters from backtest [Date] successfully applied to active strategy via Version Manager."

Please begin by outputting the requested execution plan for Phase 1.