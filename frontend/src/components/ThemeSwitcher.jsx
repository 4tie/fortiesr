// ThemeSwitcher is no longer needed as we only have one theme (glassmorphism)
// This component is kept for backward compatibility but renders nothing
export default function ThemeSwitcher() {
  return null;
}
